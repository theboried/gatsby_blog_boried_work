const Queue = require(`better-queue`);

const queryRunner = require(`./query-runner`);

const _require = require(`../../redux`),
      store = _require.store;

const queue = new Queue((plObj, callback) => {
  const state = store.getState();
  return queryRunner(plObj, state.components[plObj.component]).then(result => callback(null, result), error => callback(error));
}, {
  concurrent: 4
});
module.exports = queue;
//# sourceMappingURL=query-queue.js.map